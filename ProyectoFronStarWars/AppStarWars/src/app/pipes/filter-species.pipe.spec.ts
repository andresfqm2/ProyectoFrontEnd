import { FilterSpeciesPipe } from './filter-species.pipe';

describe('FilterSpeciesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSpeciesPipe();
    expect(pipe).toBeTruthy();
  });
});
