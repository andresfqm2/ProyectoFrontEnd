import { FilterStartshipsPipe } from './filter-startships.pipe';

describe('FilterStartshipsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterStartshipsPipe();
    expect(pipe).toBeTruthy();
  });
});
