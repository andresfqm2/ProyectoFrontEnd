import { FilterPlanetsPipe } from './filter-planets.pipe';

describe('FilterPlanetsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPlanetsPipe();
    expect(pipe).toBeTruthy();
  });
});
